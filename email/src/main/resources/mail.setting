# 邮件服务器的SMTP地址，可选，默认为smtp.<发件人邮箱后缀>
host = smtp.qq.com
# 邮件服务器的SMTP端口，可选
# port = 587
# 发件人（必须正确，否则发送失败）
from = MP项目<1017459962@qq.com>
# 用户名，默认为发件人邮箱前缀
user = 1017459962@qq.com
# 密码（注意，某些邮箱需要为SMTP服务单独设置授权码，详情查看相关帮助）
pass = fpzchkdyoravbdfg
# 使用SSL安全连接
sslEnable = true
# SMTP超时时长，单位毫秒，缺省值不超时
timeout = 0
# Socket连接超时值，单位毫秒，缺省值不超时
connectionTimeout = 0
