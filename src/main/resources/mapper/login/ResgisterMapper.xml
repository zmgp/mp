<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shu.ming.mp.modules.login.mapper.RegisterMapper">

    <insert id="insertOneUser" parameterType="com.shu.ming.mp.modules.login.bean.UserInfo" useGeneratedKeys="true">
        insert into user_info(username,password,email)
        values(#{username},#{password},#{email})
    </insert>
</mapper>
